# Supabase CLI config. Run commands from the repo root.
#   npx supabase link --project-ref tzsheugwqnzhvajphctl
#   npx supabase db push

project_id = "tzsheugwqnzhvajphctl"

[db]
# Must match the Postgres major version of the linked project so that local
# development and `db diff` behave the same as production.
major_version = 15
